language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t mosleim/praktekdocker -f Dockerfile.dev .

script:
  - docker run -e CI=true  mosleim/praktekdocker CI=true npm run test
